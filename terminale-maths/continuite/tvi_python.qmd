---
title: "Algorithme de dichotomie et python"
format:
  html:
    toc: true
    number-sections: false
include-in-header:
  - text: |
      <link rel="stylesheet" href="../../assets/css/pyodide-mkdocs.css">
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/codemirror@5.65.16/lib/codemirror.css">
      <script src="https://cdn.jsdelivr.net/npm/codemirror@5.65.16/lib/codemirror.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/codemirror@5.65.16/mode/python/python.js"></script>
      <script src="https://cdn.jsdelivr.net/pyodide/v0.26.4/full/pyodide.js"></script>
      <script src="../../assets/js/pyodide-mkdocs.js" defer></script>
description: "programmation de l'algorithme de dichotomie"
order: 5
---


_____





::: {.bloc}
Une des démonstrations du théorème des valeurs intermédiaires repose sur la construction de suites adjacentes et sur le principe de dichotomie. L'idée est de {partager} l'intervalle d'étude en deux et de regarder sur quel intervalle se trouve la solution.

Dans ce qui suit, on va supposer une fonction $f$ continue et strictement monotone sur un intervalle $[a \, ; \, b]$. On sait de plus qu'elle a une unique racine sur cet intervalle.

La stricte monotonie et l'existence d'une racine {imposent} la condition de départ : $f(a)f(b) < 0$.

1. On pose $c = \frac{a+b}{2}$
    - Si $f(c)=0$ alors c'est terminé
    - Sinon, si $f(a) f(c) < 0$ alors $f(a)$ et $f(c)$ sont de signes contraires et donc $f$ s'annule entre $a$
    - Sinon $f(a) \times f(c) > 0$ alors $f(a)$ et $f(c)$ sont de même signe et donc $f$ ne s'annule pas (à priori) entre $a$ et $c$, ce qui signifie que $f$ s'annule entre $c$ et $b$.
 
2. 
   - Si $f$ s'annule entre $a$ et $c$, on pose $$a:= a \qquad \text{ et } \qquad b:=c$$
   - Si $f$ s'annule entre $c$ et $b$, on pose $$a:=c \qquad \text{ et } \qquad b:=b$$

3. On recommence avec les nouvelles valeurs de $a$ et $b$.
:::


::: {.bloc}
On souhaite approcher la solution le plus près possible. Il est donc nécessaire de disposer d'une boucle afin de réaliser plusieurs fois le même procédé et d'\textcolor{red}{imposer} une condition de sortie lorsque l'on estime que l'intervalle $[a \, ; \, b]$ obtenu est d'amplitude suffisamment faible pour que l'on estime avoir une bonne valeur approchée de la solution.
:::


~~~python
def dichotomie(f,a,b,n):
	while abs(a-b)>10**(-n):
		c=(a+b)/2
		if f(c)==0:
			return c
		elif f(a)*f(c) < 0:
			b=c
		else :
			a=c
	return a,b
~~~

~~~{=html}
<div class="exo-wrap">
  <div class="editor-shell" id="exo1">
    <div class="bar-top"><span class="cell-num">Somme des termes</span></div>
    <textarea id="code">
from math import exp

def dichotomie(f,a,b,n):
	while abs(a-b)>10**(-n):
		c=(a+b)/2
		if f(c)==0:
			return c
		elif f(a)*f(c) < 0:
			b=c
		else :
			a=c
	return a,b

def fonction(x):
    return exp(x)*x-1

print(dichotomie(fonction,0,1,5))
</textarea>
    <div class="output" id="out"><pre></pre></div>
    <div class="toolbar">
      <button class="btn" id="run" disabled></button>
      <button class="btn" id="check" disabled></button>
      <button class="btn" id="download"></button>
      <button class="btn" id="reset"></button>
      <button class="btn" id="clear"></button>
      <button class="btn" id="save"></button>
    </div>
  </div>
</div>
~~~



::: {.bloc .rem}
<span class="bloc-titre">Commentaire sur le programme :</span><br>

- `f,a,b,n` sont les paramètres de la fonction `dichotomie()`
- `f` est la fonction sur laquelle on va appliquer le programme de dichotomie ;
- `a` et `b` sont les bornes **initiales** de l'intervalle sur lequel on cherche une solution ;
- `n` va indiquer la précision souhaitée.
:::




<div class="nav-pages">
 <a class="prev" href="tvi.html">⬅️</a>
  <a class="next" href="caracterisation.html">➡️</a>
</div>
